# 🔌 TMF Insertion Implementation

This directory contains the code for the **Advisory & Limited Authority Override Layers**.

### Engineering Goal
Integrate TMF layers into an existing perception-planning pipeline at defined interface points. This layer injects the binary mask ($M_{phys} \times M_{sem}$) into the control loop to act as a rigid safety envelope.
